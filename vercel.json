{
  "buildCommand": "pnpm install && pnpm --filter @workspace/api-server exec node build-serverless.mjs && pnpm --filter @workspace/earn-at-home exec vite build --config vite.config.ts --outDir ../../dist --emptyOutDir",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "functions": {
    "api/index.js": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.js" },
    { "source": "/((?!.*\\.).*)", "destination": "/index.html" }
  ]
}
